<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Windows 10 Style Website</title>
  <style>
    body {
      margin: 0;
      font-family: Segoe UI, Tahoma, Geneva, Verdana, sans-serif;
      background: url('https://wallpaperaccess.com/full/2061704.jpg') no-repeat center center fixed;
      background-size: cover;
      color: white;
    }

    /* Taskbar */
    .taskbar {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      height: 40px;
      background-color: rgba(0,0,0,0.7);
      display: flex;
      align-items: center;
      padding: 0 10px;
    }

    .start-button {
      width: 40px;
      height: 40px;
      background: url('https://upload.wikimedia.org/wikipedia/commons/thumb/4/48/Windows_logo_-_2012.svg/32px-Windows_logo_-_2012.svg.png') no-repeat center center;
      background-size: 24px;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .start-button:hover {
      background-color: rgba(255,255,255,0.1);
      border-radius: 4px;
    }

    .taskbar-icons {
      display: flex;
      margin-left: 10px;
      gap: 10px;
    }

    .taskbar-icon {
      width: 32px;
      height: 32px;
      background-color: rgba(255,255,255,0.1);
      border-radius: 4px;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .taskbar-icon:hover {
      background-color: rgba(255,255,255,0.3);
    }

    /* Example window */
    .window {
      position: absolute;
      top: 50px;
      left: 50px;
      width: 300px;
      background-color: rgba(30,30,30,0.95);
      border: 1px solid #444;
      border-radius: 6px;
      overflow: hidden;
      box-shadow: 0 0 10px #000;
    }

    .window-header {
      background-color: #0078d7;
      padding: 8px;
      cursor: move;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .window-header h1 {
      font-size: 14px;
      margin: 0;
    }

    .window-content {
      padding: 10px;
      font-size: 14px;
      color: #ddd;
    }

    .close-button {
      width: 20px;
      height: 20px;
      background-color: red;
      color: white;
      text-align: center;
      line-height: 20px;
      border-radius: 2px;
      cursor: pointer;
      font-weight: bold;
    }

    .close-button:hover {
      background-color: darkred;
    }
  </style>
</head>
<body>
  <!-- Example Window -->
  <div class="window" id="myWindow">
    <div class="window-header">
      <h1>Mein Fenster</h1>
      <div class="close-button" onclick="document.getElementById('myWindow').style.display='none';">×</div>
    </div>
    <div class="window-content">
      Willkommen in deiner Windows-10-artigen Webseite!
    </div>
  </div>

  <!-- Taskbar -->
  <div class="taskbar">
    <div class="start-button" title="Start"></div>
    <div class="taskbar-icons">
      <div class="taskbar-icon" title="Explorer">📁</div>
      <div class="taskbar-icon" title="Browser">🌐</div>
      <div class="taskbar-icon" title="Mail">✉️</div>
    </div>
  </div>
</body>
</html>
